import { DivstructureDirective } from './divstructure.directive';

describe('DivstructureDirective', () => {
  it('should create an instance', () => {
    const directive = new DivstructureDirective();
    expect(directive).toBeTruthy();
  });
});
